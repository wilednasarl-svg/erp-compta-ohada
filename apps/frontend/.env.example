# Public — bundled into the client at build time. Points the API client
# at the NestJS backend. In dev: localhost:3001. In Vercel / prod: set
# to the deployed backend origin (e.g. https://api.erp-compta.io).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
